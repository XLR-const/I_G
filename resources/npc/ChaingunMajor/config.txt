# Визуальные настройки пулеметчика ChaingunMajor
scale = 0.75
animation_speed = 160   # Шагает тяжело и размеренно
death_speed = 110       # Кадры падения трупа

# Настройки физического хитбокса
radius = 0.40           # Чуть шире обычного, чтобы сложнее было обойти в узком проходе
